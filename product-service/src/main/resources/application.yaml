eureka:
  client:
    service-url:
      defaultZone: http://eureka-server-svc:8761/eureka/
spring:
  data:
    mongodb:
      host: mongo-svc
      port: 27017
      username: ${username}
      password: ${password}
      uri: ${mongo_uri}
      database: product-db
      authentication-database: admin
  application:
    name: product-service
server:
  port: 8082
file.receiver.url: http://file-receiver-svc:8083/api/v2/file/photo
