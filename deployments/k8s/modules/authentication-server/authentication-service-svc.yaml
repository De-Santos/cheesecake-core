apiVersion: v1
kind: Service
metadata:
  name: authentication-svc
  namespace: default
spec:
  selector:
    app: authentication-service
  type: NodePort
  ports:
    - port: 8085
      targetPort: 8085
